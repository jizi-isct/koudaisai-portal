{
  "name": "koudaisai-portal",
  "parallel": 5,
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nx run-many --target dev -p backend portal @koudaisai-portal/admin join --parallel"
      }
    },
    "local-registry": {
      "executor": "@nx/js:verdaccio",
      "options": {
        "port": 4873,
        "config": ".verdaccio/config.yml",
        "storage": "tmp/local-registry/storage"
      }
    }
  }
}
