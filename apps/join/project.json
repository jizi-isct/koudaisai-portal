{
  "name": "join",
  "sourceRoot": "apps/join/src",
  "projectType": "application",
  "implicitDependencies": [
    "@koudaisai-portal/shared-ui-forms",
    "@koudaisai/shared-api"
  ],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "astro build",
        "cwd": "apps/join"
      },
      "dependsOn": ["^build"]
    },
    "deploy:production": {
      "executor": "nx:run-commands",
      "options": {
        "command": "wrangler deploy",
        "cwd": "apps/join"
      },
      "cache": false,
      "dependsOn": ["build"]
    },
    "deploy:preview": {
      "executor": "nx:run-commands",
      "cache": false,
      "options": {
        "command": "wrangler deploy --env preview",
        "cwd": "apps/join"
      },
      "dependsOn": ["build"]
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "astro dev --host 127.0.0.1 --port 3002",
        "cwd": "apps/join"
      }
    }
  }
}
