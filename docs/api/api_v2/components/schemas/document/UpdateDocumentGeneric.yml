type: object
properties:
  title:
    type: string
  category:
    type: string
    format: uuid
    nullable: true
  targets:
    type: array
    description: Target specifiers for the form
    items:
      $ref: '../common/TargetSpecifier.yml'
